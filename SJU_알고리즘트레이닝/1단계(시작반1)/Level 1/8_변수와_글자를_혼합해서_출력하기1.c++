#include <iostream>
#include <algorithm>
#include <string>
#include <vector>

using namespace std;



int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int a = 5, b = -1, c = 2;
    cout << "a, b, c 값은 각각 a는 " << a << ", b는 " << b << ", c는 " << c << " 입니다";
    
    return 0;
}